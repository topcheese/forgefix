# Runtime state files (but keep forgefix_ledger.json — FF needs it to identify project)
.ff/*
!.ff/forgefix_ledger.json

# Binaries
ff
forgefix
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
tmp/
/build/

# Config files (contain secrets — use .env or template copies)
forgefix_ff.yaml
*_bak.yaml

# OpenCode agent configuration (dev tooling, not part of ForgeFix)
.opencode/

# Swarm agent runtime state
.swarm/
bak.opencode/
bak.swarm/
repair_specs_tmp.py
test_spec_id.txt
specs/Fix ff commit malformed spec frontmatter and stale linked_commits.md
